amqpinput:
  serveruri: "amqp://guest:guest@localhost:5672/" 
  dryrun: true
  maxdryrunmsgs: 10

  channels:
    - name: "test"
      required: true
      maxbatchsize: 100
      minintervalms: 100
      maxintervalms: 1000
